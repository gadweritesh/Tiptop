<?xml version="1.0" encoding="utf-8" ?>

<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:thickness="0dp">
    <stroke
        android:width="3dp"
        android:color="@color/colorLoginButton" />
    <corners android:radius="5dp" />
    <gradient
        android:angle="270"
        android:centerColor="@color/colorLoginButton"
        android:endColor="@color/colorLoginButton"
        android:startColor="@color/colorLoginButton"
        android:type="linear" />

</shape>